════════════════════════════════════════════════════════════════
  🚀 تشغيل Computer Control Agent - دليل سريع
════════════════════════════════════════════════════════════════

📌 خطوات التشغيل (5 دقائق فقط!)

┌────────────────────────────────────────────────────────────┐
│ 1️⃣  احصل على API Key                                      │
└────────────────────────────────────────────────────────────┘

   الخيار 1 (موصى به): DeepSeek
   ├─ اذهب: https://platform.deepseek.com
   ├─ سجل حساب
   └─ احصل على API Key

   الخيار 2: Claude
   ├─ اذهب: https://console.anthropic.com
   └─ احصل على API Key

   الخيار 3: OpenAI
   ├─ اذهب: https://platform.openai.com
   └─ احصل على API Key

┌────────────────────────────────────────────────────────────┐
│ 2️⃣  ضع API Key في المتغيرات البيئية                       │
└────────────────────────────────────────────────────────────┘

   في الـ Terminal:

   export DEEPSEEK_API_KEY="your-key-here"

   أو في ملف .env:

   echo "DEEPSEEK_API_KEY=your-key-here" >> .env

┌────────────────────────────────────────────────────────────┐
│ 3️⃣  جرب المثال السريع                                     │
└────────────────────────────────────────────────────────────┘

   npx tsx examples/agent-quick-start.ts

┌────────────────────────────────────────────────────────────┐
│ 4️⃣  أو استخدمه في الكود                                   │
└────────────────────────────────────────────────────────────┘

   import { ComputerControlAgent } from '@oqool/shared/agents';

   const agent = new ComputerControlAgent({
     apiKeys: {
       deepseek: process.env.DEEPSEEK_API_KEY
     }
   });

   await agent.execute({
     type: 'system-command',
     command: 'ls -la',
     level: 'safe'
   });

┌────────────────────────────────────────────────────────────┐
│ 5️⃣  اختبر أنه يعمل                                        │
└────────────────────────────────────────────────────────────┘

   const isActive = agent.isAgentActive();
   console.log('الوكيل نشط:', isActive);  // يجب أن يكون true

════════════════════════════════════════════════════════════════

🎯 الأوامر الأساسية (للاستخدام الفوري)

┌─ التحكم ──────────────────────────────────────────────────┐
│ agent.execute()           - تنفيذ عملية                   │
│ agent.pause()             - إيقاف مؤقت                    │
│ agent.resume()            - استئناف                       │
│ agent.stop()              - إيقاف                         │
│ agent.emergencyStop()     - إيقاف طارئ                    │
└────────────────────────────────────────────────────────────┘

┌─ المعلومات ──────────────────────────────────────────────┐
│ agent.isAgentActive()     - هل نشط؟                      │
│ agent.getStatus()         - الحالة الكاملة               │
│ agent.getOperationHistory() - سجل العمليات               │
└────────────────────────────────────────────────────────────┘

┌─ الأمان ──────────────────────────────────────────────────┐
│ agent.createSnapshot()    - إنشاء نسخة احتياطية          │
│ agent.restoreFromSnapshot() - استعادة                    │
└────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════

⚡ مثال سريع (Copy & Paste)

import { ComputerControlAgent } from '@oqool/shared/agents';

async function test() {
  // تفعيل
  const agent = new ComputerControlAgent({
    apiKeys: { deepseek: process.env.DEEPSEEK_API_KEY }
  });

  // تنفيذ
  await agent.execute({
    type: 'system-command',
    command: 'echo "Hello!"',
    level: 'safe',
    description: 'اختبار'
  });

  console.log('✅ يعمل!');
}

test();

════════════════════════════════════════════════════════════════

📚 للتفاصيل الكاملة:

├─ HOW_TO_ACTIVATE_AGENT.md    ← دليل التفعيل الكامل
├─ COMPUTER_CONTROL_COMMANDS.md ← جميع الأوامر مع شرح
├─ COMMANDS_LIST.txt           ← قائمة الأوامر فقط
└─ examples/agent-quick-start.ts ← مثال جاهز للتشغيل

════════════════════════════════════════════════════════════════

❓ مشاكل شائعة

❌ "Cannot find module"
   → cd /media/amir/MO881/oqool-monorepo
   → npm install && npm run build

❌ "API Key not found"
   → export DEEPSEEK_API_KEY="your-key"

❌ "Permission denied"
   → استخدم sudo: 'confirm' في securityPolicy

════════════════════════════════════════════════════════════════

✅ الآن أنت جاهز!

شغل المثال السريع:
→ npx tsx examples/agent-quick-start.ts

════════════════════════════════════════════════════════════════
