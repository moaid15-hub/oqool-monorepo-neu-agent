# 🌟 Arabic Agent - وكيل الذكاء الاصطناعي العربي

> **نظام متطور لفهم اللغة العربية وتحويلها إلى كود برمجي احترافي**  
> مع **توفير 70-80% من التكاليف** عبر نظام Multi-Provider ذكي

[![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-blue.svg)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Arabic](https://img.shields.io/badge/Language-Arabic-success.svg)](README.md)

---

## 📋 المحتويات

- [🎯 نظرة عامة](#-نظرة-عامة)
- [✨ الميزات الرئيسية](#-الميزات-الرئيسية)
- [💰 توفير التكاليف](#-توفير-التكاليف)
- [🚀 البدء السريع](#-البدء-السريع)
- [📚 الاستخدام](#-الاستخدام)
- [🔧 التكوين المتقدم](#-التكوين-المتقدم)
- [📖 الأمثلة](#-الأمثلة)
- [🏗️ المعمارية](#️-المعمارية)
- [🤝 المساهمة](#-المساهمة)

---

## 🎯 نظرة عامة

**Arabic Agent** هو نظام ذكاء اصطناعي متطور مُصمم خصيصاً للمطورين العرب، يجمع بين:

- 🧠 **فهم عميق للغة العربية** - معالجة طبيعية متقدمة للغة العربية
- 💻 **توليد كود احترافي** - كتابة كود نظيف وموثق بالكامل
- 🔄 **Multi-Provider System** - دعم Anthropic Claude, OpenAI, DeepSeek, Google AI
- 💰 **توفير 70-80%** - من التكاليف عبر Intelligent Routing
- 🎓 **تعليمي** - شرح المفاهيم بطريقة واضحة ومبسطة

---

## ✨ الميزات الرئيسية

### 🌐 فهم اللغة العربية

```typescript
// فهم متطلب عربي وتحويله لمعمارية تقنية كاملة
const architecture = await agent.understandArabicRequirement(`
أريد تطبيق لإدارة المطاعم يتضمن:
- نظام حجز الطاولات
- إدارة القائمة والطلبات  
- نظام دفع إلكتروني
- تطبيق موبايل للعملاء
`);
```

### 💻 تحويل الأفكار إلى كود

```typescript
// تحويل فكرة عربية إلى كود عملي
const code = await agent.ideaToCode(
  'اكتب API لإدارة المستخدمين مع مصادقة JWT',
  'javascript',
  'medium'
);
```

### 📖 شرح الكود بالعربية

```typescript
// شرح أي كود بطريقة تعليمية مفصلة
const explanation = await agent.explainCodeInArabic(
  codeFile,
  'intermediate' // مبتدئ | متوسط | متقدم
);
```

### 🐛 تحليل الأخطاء

```typescript
// تحليل وشرح الأخطاء البرمجية مع حلول
const analysis = await agent.explainError(
  errorMessage,
  buggyCode,
  'javascript'
);
```

### 💡 توليد أمثلة عملية

```typescript
// توليد أمثلة برمجية واقعية
const example = await agent.generateExample(
  'Async/Await في JavaScript',
  'javascript',
  'backend'
);
```

### 🧠 استخراج النية (Intent Recognition)

```typescript
// فهم ما يريده المستخدم من جملة عربية
const intent = await agent.extractIntent(
  'اكتب لي API بسيط بـ Express'
);
// { intent: 'create_project', entities: [...], confidence: 0.95 }
```

### 💬 محادثة تفاعلية

```typescript
// محادثة مع سياق محفوظ
const response = await agent.chat('كيف أحسن أداء React؟');
```

### 🔤 ترجمة المصطلحات

```typescript
// ترجمة مصطلحات برمجية من العربية للإنجليزية
const term = agent.translateTerm('دالة'); // 'function'
const code = agent.translateCodeText('أنشئ دالة جديدة'); // 'create new function'
```

---

## 💰 توفير التكاليف

### كيف يعمل نظام التوفير؟

```
┌─────────────────────────────────────────────────────────────┐
│                 Intelligent Provider Routing                 │
└─────────────────────────────────────────────────────────────┘

 المهمة البسيطة (Simple)
 ┌──────────────────┐
 │   DeepSeek AI    │  ← أرخص مزود ($0.001/1K tokens)
 │   $0.001/1K      │     مناسب للمهام البسيطة
 └──────────────────┘

 المهمة المتوسطة (Medium)
 ┌──────────────────┐
 │   OpenAI GPT-4o  │  ← متوسط السعر ($0.006/1K tokens)
 │   $0.006/1K      │     توازن بين السعر والجودة
 └──────────────────┘

 المهمة المعقدة (Complex)
 ┌──────────────────┐
 │ Anthropic Claude │  ← أعلى جودة ($0.015/1K tokens)
 │   $0.015/1K      │     للمهام المعقدة والحساسة
 └──────────────────┘

 + Smart Caching (توفير إضافي 40-60%)
 + Automatic Fallback (موثوقية عالية)
 + Retry Logic (معالجة ذكية للأخطاء)
```

### مثال عملي للتوفير

```typescript
// ❌ الطريقة التقليدية (Claude فقط)
const traditionalConfig = {
  providers: [{ name: 'anthropic', apiKey: '...' }],
  costOptimization: false
};
// التكلفة: $0.15 لـ 10K tokens

// ✅ مع Arabic Agent (Multi-Provider)
const optimizedConfig = {
  providers: [
    { name: 'deepseek', apiKey: '...' },  // للبسيط
    { name: 'openai', apiKey: '...' },    // للمتوسط
    { name: 'anthropic', apiKey: '...' }  // للمعقد
  ],
  costOptimization: true
};
// التكلفة: $0.03 لـ 10K tokens

// 💰 التوفير: 80% = $0.12
```

---

## 🚀 البدء السريع

### التثبيت

```bash
npm install @oqool/arabic-agent
# أو
yarn add @oqool/arabic-agent
```

### الإعداد الأساسي

```typescript
import { ArabicAgent } from '@oqool/arabic-agent';

// إعداد بسيط (مزود واحد)
const agent = new ArabicAgent({
  providers: [{
    name: 'anthropic',
    apiKey: process.env.ANTHROPIC_API_KEY
  }]
});

// الاستخدام
const code = await agent.ideaToCode(
  'اكتب دالة للبحث الثنائي',
  'javascript'
);

console.log(code.content);
```

### الإعداد المُحسّن (Multi-Provider)

```typescript
const agent = new ArabicAgent({
  providers: [
    {
      name: 'deepseek',
      apiKey: process.env.DEEPSEEK_API_KEY,
      baseURL: 'https://api.deepseek.com/v1'
    },
    {
      name: 'openai',
      apiKey: process.env.OPENAI_API_KEY
    },
    {
      name: 'anthropic',
      apiKey: process.env.ANTHROPIC_API_KEY
    }
  ],
  defaultProvider: 'deepseek',
  fallbackEnabled: true,
  costOptimization: true,
  retryAttempts: 3
});

// الآن يوفر 70-80% تلقائياً!
```

---

## 📚 الاستخدام

### 1. فهم المتطلبات العربية

```typescript
const requirement = `
أريد نظام إدارة مكتبة يتضمن:
- تسجيل الكتب والمؤلفين
- استعارة وإرجاع الكتب
- تتبع المستخدمين
- واجهة ويب وتطبيق موبايل
`;

const architecture = await agent.understandArabicRequirement(requirement);

console.log(architecture);
// {
//   components: [...],
//   api: { endpoints: [...], authentication: 'JWT' },
//   database: { type: 'PostgreSQL', tables: [...] },
//   frontend: { framework: 'React', ... },
//   technologies: { ... }
// }
```

### 2. توليد الكود

```typescript
// مثال بسيط
const simpleCode = await agent.ideaToCode(
  'دالة لحساب مجموع مصفوفة من الأرقام',
  'javascript',
  'simple'  // مستوى التعقيد
);

// مثال معقد
const complexCode = await agent.ideaToCode(
  'نظام مصادقة JWT كامل مع refresh tokens',
  'typescript',
  'complex'
);

console.log(complexCode.content);
```

### 3. شرح الكود

```typescript
const codeToExplain = {
  path: 'algorithm.js',
  language: 'javascript',
  lines: 25,
  content: `/* كود معقد هنا */`
};

const explanation = await agent.explainCodeInArabic(
  codeToExplain,
  'beginner'  // مستوى الشرح
);

console.log(explanation);
// يحتوي على:
// - الغرض من الكود
// - شرح سطر بسطر
// - المفاهيم البرمجية
// - نقاط القوة والضعف
// - اقتراحات للتحسين
```

### 4. تحليل الأخطاء

```typescript
const errorMessage = "TypeError: Cannot read property 'name' of undefined";
const buggyCode = `
const user = null;
console.log(user.name); // Error!
`;

const analysis = await agent.explainError(
  errorMessage,
  buggyCode,
  'javascript'
);

console.log(analysis);
// يحتوي على:
// - تشخيص الخطأ
// - شرح بالعربية
// - الحل المقترح
// - كيفية تجنبه مستقبلاً
```

### 5. توليد أمثلة

```typescript
const example = await agent.generateExample(
  'Redux Toolkit',
  'javascript',
  'web'  // السياق
);

console.log(example.code);        // الكود
console.log(example.explanation); // الشرح
```

### 6. المحادثة التفاعلية

```typescript
// محادثة مع سياق محفوظ
await agent.chat('ما هو React Hooks؟');
await agent.chat('كيف أستخدم useState؟');
await agent.chat('أعطني مثال عملي');

// إعادة تعيين السياق
await agent.chat('موضوع جديد', true);
```

### 7. استخراج النية

```typescript
const result = await agent.extractIntent(
  'اكتب لي REST API للمنتجات بـ Node.js'
);

console.log(result);
// {
//   intent: 'create_project',
//   entities: [
//     { type: 'technology', value: 'Node.js' },
//     { type: 'framework', value: 'REST API' },
//     { type: 'feature', value: 'products management' }
//   ],
//   confidence: 0.95
// }
```

---

## 🔧 التكوين المتقدم

### إعدادات شاملة

```typescript
const config: MultiProviderConfig = {
  providers: [
    {
      name: 'deepseek',
      apiKey: process.env.DEEPSEEK_API_KEY,
      baseURL: 'https://api.deepseek.com/v1',
      model: 'deepseek-coder',
      maxTokens: 4096,
      temperature: 0.7
    },
    {
      name: 'openai',
      apiKey: process.env.OPENAI_API_KEY,
      model: 'gpt-4o',
      maxTokens: 4096,
      temperature: 0.7
    },
    {
      name: 'anthropic',
      apiKey: process.env.ANTHROPIC_API_KEY,
      model: 'claude-sonnet-4-20250514',
      maxTokens: 8192,
      temperature: 0.7
    }
  ],
  
  // المزود الافتراضي (للمهام البسيطة)
  defaultProvider: 'deepseek',
  
  // تفعيل Fallback التلقائي
  fallbackEnabled: true,
  
  // تفعيل تحسين التكلفة
  costOptimization: true,
  
  // عدد محاولات إعادة الطلب
  retryAttempts: 3,
  
  // وقت الانتظار الأقصى
  timeout: 30000 // 30 ثانية
};

const agent = new ArabicAgent(config);
```

### استخدام Caching

```typescript
// تفعيل الكاش (افتراضي)
const result1 = await agent.chat('ما هو TypeScript؟', {
  useCache: true  // سيتم حفظه في الكاش
});

// نفس السؤال (من الكاش - أسرع وأرخص)
const result2 = await agent.chat('ما هو TypeScript؟');

// مسح الكاش
agent.clearCache();
```

### تحديد مستوى التعقيد

```typescript
// بسيط - يستخدم DeepSeek (أرخص)
await agent.ideaToCode(prompt, 'js', 'simple');

// متوسط - يستخدم OpenAI (متوازن)
await agent.ideaToCode(prompt, 'js', 'medium');

// معقد - يستخدم Claude (أفضل جودة)
await agent.ideaToCode(prompt, 'js', 'complex');

// خبير - يستخدم Claude دائماً
await agent.ideaToCode(prompt, 'js', 'expert');
```

### إحصائيات الاستخدام

```typescript
// عرض إحصائيات شاملة
agent.displayStatistics();

// الحصول على المقاييس
const metrics = agent.getMetrics();
console.log(metrics);
// {
//   totalRequests: 50,
//   successfulRequests: 48,
//   failedRequests: 2,
//   totalCost: 0.125,
//   totalTokens: 12500,
//   averageLatency: 1234,
//   cacheHits: 15,
//   cacheMisses: 35
// }

// إعادة تعيين المقاييس
agent.resetMetrics();
```

---

## 📖 الأمثلة

### مثال 1: مشروع TODO List كامل

```typescript
// 1. فهم المتطلب
const requirement = 'تطبيق TODO List مع React و Express';
const arch = await agent.understandArabicRequirement(requirement);

// 2. توليد Backend
const backend = await agent.ideaToCode(
  'Express API لإدارة المهام (CRUD)',
  'javascript',
  'medium'
);

// 3. توليد Frontend  
const frontend = await agent.ideaToCode(
  'React component لعرض وإدارة المهام',
  'javascript',
  'medium'
);

// 4. شرح الكود
const explanation = await agent.explainCodeInArabic(backend);

// 5. إحصائيات
agent.displayStatistics();
```

### مثال 2: تحليل وإصلاح خطأ

```typescript
const error = "ReferenceError: user is not defined";
const code = `
function greetUser() {
  console.log('Hello ' + user); // user غير معرف
}
greetUser();
`;

const analysis = await agent.explainError(error, code, 'javascript');
console.log(analysis);

// النتيجة:
// 🔍 تشخيص الخطأ: متغير user غير معرف
// 💡 الشرح: المتغير user لم يتم تعريفه قبل استخدامه
// ✅ الحل: قم بتعريف المتغير أولاً أو تمريره كمعامل
// 🛡️ الوقاية: استخدم 'use strict' وأدوات Linting
```

### مثال 3: محادثة تعليمية

```typescript
// سلسلة أسئلة مترابطة
await agent.chat('ما الفرق بين Promise و Async/Await؟');
await agent.chat('متى أستخدم كل واحد؟');
await agent.chat('أعطني مثال عملي');
await agent.chat('كيف أتعامل مع الأخطاء في كل حالة؟');
```

### مثال 4: مقارنة التكلفة

```typescript
// ❌ استخدام Claude فقط
const expensiveAgent = new ArabicAgent({
  providers: [{ name: 'anthropic', apiKey: '...' }],
  costOptimization: false
});

// ✅ استخدام Multi-Provider
const cheapAgent = new ArabicAgent({
  providers: [
    { name: 'deepseek', apiKey: '...' },
    { name: 'openai', apiKey: '...' },
    { name: 'anthropic', apiKey: '...' }
  ],
  costOptimization: true
});

// تنفيذ نفس المهام
for (const task of tasks) {
  await expensiveAgent.chat(task);
  await cheapAgent.chat(task);
}

// مقارنة النتائج
const cost1 = expensiveAgent.getMetrics().totalCost;
const cost2 = cheapAgent.getMetrics().totalCost;
console.log(`التوفير: ${((cost1 - cost2) / cost1 * 100).toFixed(1)}%`);
// التوفير: 78.5%
```

---

## 🏗️ المعمارية

```
┌─────────────────────────────────────────────────────────────┐
│                      Arabic Agent                            │
│                  (واجهة المستخدم الرئيسية)                  │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                 UnifiedAIAdapter                             │
│           (المحول الموحد متعدد المزودين)                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Intelligent Router (التوجيه الذكي)                  │  │
│  │  • Cost Optimization (تحسين التكلفة)                 │  │
│  │  • Complexity Analysis (تحليل التعقيد)               │  │
│  │  │  • Provider Selection (اختيار المزود)              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Cache Manager (إدارة الكاش)                          │  │
│  │  • Smart Caching (كاش ذكي)                           │  │
│  │  • TTL Management (إدارة مدة الحفظ)                   │  │
│  │  • Memory Optimization (تحسين الذاكرة)               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Error Handler (معالج الأخطاء)                       │  │
│  │  • Retry Logic (إعادة المحاولة)                      │  │
│  │  • Automatic Fallback (تبديل تلقائي)                │  │
│  │  • Error Recovery (استرجاع من الأخطاء)              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Metrics Collector (جامع المقاييس)                   │  │
│  │  • Cost Tracking (تتبع التكلفة)                       │  │
│  │  • Performance Monitoring (مراقبة الأداء)            │  │
│  │  • Usage Analytics (تحليل الاستخدام)                 │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────┬──────────────┬──────────────┬─────────────────┘
             │              │              │
     ┌───────▼──────┐ ┌────▼─────┐ ┌──────▼───────┐
     │   DeepSeek   │ │  OpenAI  │ │   Claude     │
     │  (أرخص)     │ │ (متوازن)  │ │ (أفضل جودة)  │
     │  $0.001/1K   │ │ $0.006/1K │ │  $0.015/1K   │
     └──────────────┘ └──────────┘ └──────────────┘
```

---

## 🎯 حالات الاستخدام

### للمطورين المبتدئين
- 📚 **تعلم البرمجة بالعربية** - شرح المفاهيم بطريقة مبسطة
- 💡 **توليد أمثلة عملية** - أمثلة واقعية من البيئة العربية
- 🐛 **فهم الأخطاء** - شرح الأخطاء وكيفية حلها

### للمطورين المحترفين
- 🚀 **تسريع التطوير** - توليد كود نظيف وموثق
- 🏗️ **تصميم المعماريات** - تحويل المتطلبات لمعمارية تقنية
- 📝 **توثيق الكود** - توليد وثائق شاملة بالعربية

### للفرق والشركات
- 💰 **توفير التكاليف** - تقليل 70-80% من تكاليف AI
- 🔄 **سير عمل موحد** - نظام موحد لجميع أعضاء الفريق
- 📊 **تحليل الأداء** - مقاييس دقيقة للاستخدام والتكلفة

---

## 🔐 الأمان والخصوصية

- 🔒 **مفاتيح API آمنة** - تُحفظ محلياً فقط
- 🚫 **لا تخزين سحابي** - لا يتم إرسال بيانات لخوادمنا
- 🛡️ **اتصالات مشفرة** - HTTPS لجميع الاتصالات
- 📝 **لا logging** - لا نسجل محتوى طلباتك

---

## 📊 الإحصائيات والمقاييس

```typescript
// عرض إحصائيات شاملة
agent.displayStatistics();

// مثال للناتج:
// 💰 تحليل التكلفة والتوفير:
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 📊 إجمالي الطلبات: 150
// ✅ نجح: 147
// ❌ فشل: 3
// 💾 Cache Hits: 45 (30.0%)
// 💵 التكلفة الفعلية: $0.0345
// 💰 التوفير المقدر: $0.2588 (75%)
// ⚡ متوسط زمن الاستجابة: 1234ms
// 🪙 إجمالي التوكنز: 23,450
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🤝 المساهمة

نرحب بمساهماتكم! 🎉

### كيفية المساهمة

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit تغييراتك (`git commit -m 'إضافة ميزة رائعة'`)
4. Push للـ branch (`git push origin feature/amazing-feature`)
5. افتح Pull Request

### المبادئ التوجيهية

- 📝 اكتب كود نظيف وموثق
- ✅ أضف tests للميزات الجديدة
- 📚 حدّث الوثائق
- 🌐 دعم اللغتين العربية والإنجليزية

---

## 📞 الدعم والتواصل

- 📧 **البريد الإلكتروني**: support@oqool.ai
- 💬 **Discord**: [انضم لمجتمعنا](https://discord.gg/oqool)
- 🐛 **Issues**: [GitHub Issues](https://github.com/oqool/arabic-agent/issues)
- 📖 **التوثيق**: [docs.oqool.ai](https://docs.oqool.ai)

---

## 📜 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 🙏 شكر خاص

- 🧠 **Anthropic** - لـ Claude AI المذهل
- 🤖 **OpenAI** - لـ GPT Models
- 🔮 **DeepSeek** - لمساهمتهم في democratizing AI
- 🌟 **المساهمين** - كل من ساهم في هذا المشروع

---

## 🎓 موارد إضافية

- 📚 [دليل الاستخدام الكامل](docs/GUIDE.md)
- 🎥 [فيديوهات تعليمية](https://youtube.com/@oqool)
- 📖 [أمثلة متقدمة](examples/)
- 🔧 [دليل المطورين](docs/DEVELOPMENT.md)

---

<div align="center">

### صُنع بـ ❤️ للمطورين العرب

**[الموقع](https://oqool.ai)** • **[التوثيق](https://docs.oqool.ai)** • **[الأمثلة](examples/)** • **[المدونة](https://blog.oqool.ai)**

</div>
